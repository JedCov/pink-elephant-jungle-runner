{
  "name": "pink-elephant-jungle-runner2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && python3 -m http.server 5173 --bind 0.0.0.0"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
